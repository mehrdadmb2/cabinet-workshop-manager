name = "cabinet-backup-worker"
main = "worker.js"
compatibility_date = "2026-07-28"

[vars]
# Browser Origin only (no trailing slash/path).
ALLOWED_ORIGINS = "https://mehrdadmb2.github.io"

[observability]
enabled = true

[secrets]
required = ["TELEGRAM_BOT_TOKEN", "TELEGRAM_CHAT_ID"]
