name = "cabinet-backup-worker"
main = "worker.js"
compatibility_date = "2026-07-28"

[vars]
# Set this to the exact origin you want to allow, for example:
# ALLOWED_ORIGIN = "https://mehrdadmb2.github.io"

[observability]
enabled = true

[secrets]
required = ["TELEGRAM_BOT_TOKEN", "TELEGRAM_CHAT_ID"]
